workflows:
  ios-build:
    name: iOS Build Workflow
    instance_type: mac_mini_m2
    max_build_duration: 60
    scripts:
      - name: Say Hello
        script: echo "Starting iOS Build for goconnecthub"
    artifacts:
      - build/ios/ipa/*.ipa
